get:
  tags:
  - VendorPayments
  summary: GET VendorPayments(id)
  description: Retrieve all or some selected properties from an instance of 'VendorPayments' with the given id.
  operationId: get_VendorPayments_id
  responses:
    '200':
      description: Success
    '204':
      description: No content (update/delete)
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '404':
      description: Not found
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
  x-example-url: GET https://localhost:50000/b1s/v2/VendorPayments(123)
  x-resource-description: This entity enables you to manipulate 'VendorPayments'. It represents incoming payments to vendors
    or, for returned goods, to customers. Available payment methods are cash, credit cards, checks, bank transfers, and in
    some localizations, bills of exchange.
patch:
  tags:
  - VendorPayments
  summary: PATCH VendorPayments(id)
  description: Update an instance of 'VendorPayments' with the given payload of type 'Payment' in JSON format and with the
    specified id.
  operationId: patch_VendorPayments_id
  responses:
    '200':
      description: Success
    '204':
      description: No content (update/delete)
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '404':
      description: Not found
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            Remarks:
              type: string
              example: new comments - modified by Service Layer
        example:
          Remarks: new comments - modified by Service Layer
  x-example-url: PATCH https://localhost:50000/b1s/v2/VendorPayments(123)
  x-resource-description: This entity enables you to manipulate 'VendorPayments'. It represents incoming payments to vendors
    or, for returned goods, to customers. Available payment methods are cash, credit cards, checks, bank transfers, and in
    some localizations, bills of exchange.
