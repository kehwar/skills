{
  "doctype": "Pricing Rule",
  "group_by_field": null,
  "code_fields": [
    {
      "field": "condition",
      "filename": "condition.py"
    }
  ],
  "filters": {
    "condition": ["!=", ""]
  },
  "description": "Pricing Rule conditions are Python expressions evaluated via safe_eval to decide whether a pricing rule applies to a transaction."
}
