get:
  tags:
  - AlertManagements
  summary: GET AlertManagements(id)
  description: Retrieve all or some selected properties from an instance of 'AlertManagements' with the given id.
  operationId: get_AlertManagements_id
  responses:
    '200':
      description: Success
    '204':
      description: No content (update/delete)
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '404':
      description: Not found
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
  x-example-url: GET https://localhost:50000/b1s/v2/AlertManagements(123)
  x-resource-description: This entity enables you to manipulate 'AlertManagements'. For each alert, you need to define its
    priority, the users and documents to which the alert applies, and the conditions that trigger the alert. In addition to
    activating system alerts, you can also add alerts based on user-defined queries.
patch:
  tags:
  - AlertManagements
  summary: PATCH AlertManagements(id)
  description: Update an instance of 'AlertManagements' with the given payload of type 'AlertManagement' in JSON format and
    with the specified id.
  operationId: patch_AlertManagements_id
  responses:
    '200':
      description: Success
    '204':
      description: No content (update/delete)
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '404':
      description: Not found
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            FrequencyType:
              type: string
              example: atfi_Weeks
        example:
          FrequencyType: atfi_Weeks
  x-example-url: PATCH https://localhost:50000/b1s/v2/AlertManagements(123)
  x-resource-description: This entity enables you to manipulate 'AlertManagements'. For each alert, you need to define its
    priority, the users and documents to which the alert applies, and the conditions that trigger the alert. In addition to
    activating system alerts, you can also add alerts based on user-defined queries.
delete:
  tags:
  - AlertManagements
  summary: DELETE AlertManagements(id)
  description: Delete an instance of 'AlertManagements' with the specified id.
  operationId: delete_AlertManagements_id
  responses:
    '200':
      description: Success
    '204':
      description: No content (update/delete)
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '404':
      description: Not found
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
  x-example-url: DELETE https://localhost:50000/b1s/v2/AlertManagements(123)
  x-resource-description: This entity enables you to manipulate 'AlertManagements'. For each alert, you need to define its
    priority, the users and documents to which the alert applies, and the conditions that trigger the alert. In addition to
    activating system alerts, you can also add alerts based on user-defined queries.
